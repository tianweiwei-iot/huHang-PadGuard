# 2026-08-30

- 基于《平板管控系统-管控端APK功能设计说明书》(docx) 产出管控端 UI 精简原型：
  - 交付物：`docs/control-end-prototype.html`（Android 手机框 + 多屏可点击原型）、`docs/管控端UI精简设计说明.md`
  - 精简方案：原 8 大模块/P0-P2 → 4 底部标签（设备/管控/数据/我的）+ 告警铃铛 + 设备详情；P2 重功能仅留入口
  - 借鉴 UI：Family Link（标签+设备卡）、屏幕时间/健康使用平板（时长圆环）、Qustodio（仪表盘卡）、Family Safety（定位卡）、iOS设置（分段+开关）
  - 配色：主色 #3B6EF6，状态绿/红/琥珀；建议 Jetpack Compose + Material 3 落地

- 第二轮优化（用户二次确认 4 项决策后）：范围升为 **P0+P1 全量**、**家庭/校园双场景独立布局**（场景切换联动主题色 校园蓝#3B6EF6 / 家庭橙#F5874E + 导航文案 设备↔孩子、数据↔学情）、品牌名「护航管控」、新增**启动/登录页**（手机号验证码+密码+场景选择）。新增屏：实时屏幕+多设备轮巡、定位轨迹+电子围栏、批量指令栏。P2 重功能仍仅留入口。原型与设计说明均已同步更新。

- 第三轮：用户要求**去掉校园端，仅保留家庭（家长）场景并按文档优化**。已重写原型为纯家长端：移除登录场景选择/班级列表/多选批量/轮巡/班级报表/校区围栏/子账号(校园)；主题固定温和橙 #F5874E；补充家庭专属能力（扫码绑定、防沉迷圆环、临时授权、家庭多人共管、分时段家庭模板 上学日/周末/假期/夜间锁机、实时行为动态、家庭电子围栏、学情周报）。底部导航：孩子/管控/学情/我的。设计说明同步覆盖。

- 第四轮「转 Android 代码」：把纯家长端原型落成真实 Jetpack Compose（沿用既有 Material3/Hilt/导航约定，包 `com.padguard.presentation.ui.*`）。
  - 新增 ViewModel：`DeviceDetailViewModel`（设备详情+今日时长+位置+远程指令）、`ControlPolicyViewModel`（时长/应用/上网/系统 四栏 + 家庭预设 上学日/周末/假期/夜间锁机）、`MonitorViewModel`（实时截屏+位置）。
  - 新增屏：`ui/device/DeviceDetailScreen.kt`、`ui/control/ControlPolicyScreen.kt`（TabRow 四栏 + FilterChip 家庭模板 + Slider 时长 + AssistChip 应用限时 + 网址黑名单 + 定时锁机 + 护眼/管控模式）、`ui/monitor/MonitorScreen.kt`（LIVE 实时画面占位）。
  - 路由改造：`PadGuardNavigation.kt` 中 `Screen` 密封类改用 `route` 模式 + `create(deviceId)`，新增 `device_detail/{deviceId}`、`control/{deviceId}`、`monitor/{deviceId}` 三个 composable，HomeScreen 回调改为 `Screen.Xxx.create(id)`。ViewModel 经 `SavedStateHandle` 取 deviceId（hiltViewModel 自动注入）。
  - 注意：HomeScreen 仍保留 `SceneType.FAMILY/CAMPUS` 双场景分支与校园文案（未强制去校园引用），本次仅保证新屏纯家庭；如需彻底去校园需再清理 Models/HomeScreen/LocalDataSource 中 SCHOOL 相关枚举与 Mock。

- 编译修复（`:app:compileDebugKotlin` 已通过）：
  - `LocationInfo` 与 `PolicyTemplate` 定义在 `com.padguard.domain.repository`（在 `RepositoryInterfaces.kt` 内），不在 `com.padguard.domain.model`；ViewModel 引用需用 `repository` 包，否则 Unresolved reference 并级联导致 screen 中 `?.let{loc->...}` 类型推断失败。
  - **Compose 规则**：`@Composable` 不能在 `?.let { x -> ... }` 的非 @Composable lambda 中调用，必须改成 `if (x != null) { Composable(...) }`。
  - `Icons.Default.ScreenShare` 已弃用，新代码改用 `Icons.AutoMirrored.Filled.ScreenShare`（其余 `Icons.Default.*` 如 RestartAlt/MyLocation/VerifiedUser/HourglassBottom/School/Weekend/Celebration/Bedtime 均可用）。
  - 本机无 gradlew 脚本（仅 `gradle/wrapper/gradle-wrapper.properties`，无 jar）；可直接调用缓存分发 `C:/Users/Administrator/.gradle/wrapper/dists/gradle-8.13-bin/<hash>/gradle-8.13/bin/gradle.bat` 离线编译（`--offline`）。

- 竞品控制端 UI 调研（用户反馈当前 HomeScreen 太丑，要求"列出竞品 UI 学习"）：
  - 调研 6 款：华为健康使用平板/儿童乐园、Google Family Link(2025 Material You)、Apple 屏幕使用时间(iOS 26)、Microsoft Family Safety、Qustodio、腾讯成长守护。
  - 关键结论：当前 `HomeScreen.kt`(519 行)是密集浅蓝方块网格、混杂控制/监控、无设备切换/无"今日时长"、无底部导航、无层级留白 → 全面落后。
  - 可落地改造：顶部设备切换 pill + 今日已用/剩余时长环（Apple 圆环+华为剩余时间）+ 四大管控卡(时长/应用/上网/系统护眼→进 ControlPolicyScreen) + 监控快捷(实时画面/截屏/定位→进 MonitorScreen) + 常驻底部导航(首页/设备/统计/我的) + 异常告警置顶；视觉改浅灰底白卡圆角 16dp、单一强调色(警示红做锁屏/禁玩)、统一 material-icons-extended。
  - 已交付对比分析，待用户确认主参考风格(白底 Family Link vs 黑底腾讯成长守护)后再动手重写 HomeScreen.kt。

- UI 重构落地（用户确认"按建议执行"，采用白底 Family Link/MS Family 风格 + 底部导航 首页/设备/统计/我的）：
  - 重写 `ui/home/HomeScreen.kt`：现代仪表盘 = 顶部标题 + 孩子/设备切换 pill(横滑, 在线点) + 离线告警置顶 + 今日时长环卡(已用/剩余, 默认限额120分) + 管控四大卡(时长/应用/上网/系统护眼→ControlPolicyScreen) + 监控快捷(实时画面/截屏/定位→MonitorScreen) + 最新动态。弃用旧密集网格。
  - 新增 `ui/main/MainScreen.kt`：Scaffold + NavigationBar(4 tab) + 嵌套 NavHost；共享 `HomeViewModel`；详情/管控/监控经 rootNavController 全屏压入。
  - 新增 `ui/device/DeviceListScreen.kt`(复用 DeviceStatusCard)、`ui/statistics/StatisticsScreen.kt`(今日/本周/7日趋势柱/常用应用)、`ui/profile/ProfileScreen.kt`、`viewmodel/StatisticsViewModel.kt`。
  - `HomeViewModel` 增加 `selectedDeviceId` + `selectDevice(id)`（切换设备重载今日用量）、`refreshData` 保留选中态。
  - `PadGuardNavigation` 增加 `Screen.Main("main")`，login→main，旧 home composable 移除。
  - 离线编译 BUILD SUCCESSFUL（缓存 Gradle 8.13），修复：BorderStroke 在 foundation 包、RoundedCornerShape/FontWeight 漏导、MainScreen 漏导 foundation.layout.padding、MainScreen 调用参数名 rootNavController、CircularProgressIndicator 改 lambda progress、Logout 改 AutoMirrored。

- R10 主动重构（用户三条工程原则：后端稳定优先 / 主流稳妥手法 / 四条判定标准）。离线编译 BUILD SUCCESSFUL（Gradle 8.13，零 Kotlin 警告）：
  - 消除首页硬编码每日限额：`PolicyRepository` 新增 `getGlobalDailyLimit(deviceId): Result<Int>`（官方接口而非 UI 魔法常量）；`LocalDataSource` 实现返回 `DEFAULT_DAILY_LIMIT_MINUTES=120`（命名清晰 Mock 默认值）；`HomeViewModel` 在选中设备/刷新时一并加载并写入 `HomeUiState.dailyLimitMinutes`；`HomeScreen.TodayUsageRingCard` 改用 `uiState.dailyLimitMinutes`。
  - 集中选中设备唯一可信源：`HomeUiState` 增加 `selectedDevice`，由 ViewModel 统一解析；`HomeScreen` 与 `MainScreen` 统计页统一读取 `uiState.selectedDevice`，删去此前三处 `firstOrNull` 重复解析的隐性耦合/回归点。
  - 精简 `refreshData`：复用 `loadHomeData(showRefreshing=true)`，去除与 `loadHomeData` 重复的今日用量重载逻辑。
  - 真实退出登录：`ProfileViewModel` 调 `AuthRepository.logout()`；`MainScreen` 在 `loggedOut` 后 `rootNavController.navigate(Login)` 并 `popUpTo(Main){inclusive}`；消除 `onLogout={/*TODO*/}` 占位。
  - 消除静默空点击：`ProfileScreen` 改用 `ProfileAction` 密封类 + `onAction` 回调，可路由项接真实目标（我的孩子→设备Tab、护眼设置→Control），未实现项集中在 `MainScreen` 处理并注释预留，不再散落 `clickable(onClick={})`。
  - **校园场景结论**：本会话所附《设计说明书》含双场景（校园×31、双场景×9、场景×40），与记忆中"R3 去掉校园端"的早期简化指令冲突。依原则④（Android 与后端一致稳定）保留 `SceneType.SCHOOL`/`TEACHER` 及校园 Mock/枚举，不作为债务清理；若用户确要纯家庭端，属独立产品决策，需另立任务执行。
