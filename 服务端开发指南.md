# 护航成长 / 护航控制端 · 服务端（桥梁）开发教程

> 适用对象：平板管控系统（PadGuard）后端开发者
> 配套前端：`护航成长`（被管控端 / 孩子平板，`com.padguard.child`）、`护航控制端`（管控端 / 家长，`com.padguard.parent`）
> 版本：V1.0 ｜ 2026-09-14

---

## 0. 先回答核心问题：要不要开发服务端？

**必须开发，而且它就是你要找的"桥梁"。**

两个端目前的状态：

| 端 | 已就绪 | 缺什么 |
|---|---|---|
| 护航控制端（家长） | Retrofit 接口 + DTO 全部定义完成（`AuthApi`/`DeviceApi`/`MonitorApi`/`PolicyApi`/`StatisticsApi`/`AlertApi`/`MessageApi`/`LocationApi`） | 实际只接了 `LocalDataSource` **Mock 数据**，未连真服务；`NetworkModule` 的 Token 拦截器还是预留位 |
| 护航成长（孩子） | 完整的"被管控端↔服务端接口契约 V1.0"（MQTT topic、指令包、策略包、HTTPS 接口全定义） | 绑定用固定演示码 `123456` 走客户端 **Mock**，未真正调 `POST /device/bind`；MQTT/HTTPS 上报代码待接 |

也就是说：**两个 App 都"等一个服务端"**。没有服务端，它们互相找不到对方，系统只是两个孤立的壳。

服务端要做的事，本质就是这条链路：

```
家长(控制端) 下发指令/策略 ──HTTPS──▶ 服务端(持久化+构造指令包)
                                          │
                              ▲ MQTT 下发 │  ▼ HTTPS/WebSocket 回传
                                          │
孩子(被管控端) 执行指令+上报日志/心跳/事件 ◀──MQTT/HTTPS── 服务端
                                          │
                                          ▼ WebSocket 推送
                                 家长端实时看到设备状态/告警/截图
```

---

## 1. 两端对服务端的真实诉求（契约汇总）

### 1.1 控制端（家长）侧契约
- **Base URL**：`https://api.padguard.com/v1/`
- **鉴权**：`Authorization: Bearer <JWT>`，另含 `Refresh-Token` 头做刷新
- **统一响应**：`{ code, message, data, timestamp }`（成功 code=0）
- **实时通道**：`wss://api.padguard.com/ws?token=<jwt>`
- 接口域：`/auth` `/devices` `/monitor` `/messages` `/location` `/policies` `/statistics` `/alerts`

### 1.2 被管控端（孩子）侧契约
- **Base URL**：`https://api.padguard.cn/api/v1`
- **鉴权**：`Authorization: Bearer <deviceToken>` + `X-Device-Id` + `X-Request-Id` + `X-Timestamp`
- **统一响应**：`{ code, message, data, serverTime }`
- **双通道**：MQTT（实时：心跳/指令/回执/事件，QoS1）+ HTTPS（大包：策略/日志/截图/定位）
- 接口：`POST /device/bind` `GET /device/time` `GET /device/policy` `POST /device/logs` `POST /device/screenshot` `POST /device/locations` `GET /device/commands` `POST /device/heartbeat`

### 1.3 ⚠️ 建服务端前必须先拍板的"契约冲突"

这些冲突不解决，服务端会建出一个两端都不兼容的假桥梁：

| 冲突点 | 控制端 | 被管控端 | 处理方式 |
|---|---|---|---|
| **Base URL 前缀** | `/v1/` | `/api/v1` | 服务端**两个前缀都接**（一个网关下挂两套路由）；或后续统一两端前缀 |
| **错误码体系** | `0/1001/1002/1003/2001…` | `0/40001/40101/40301/40901/50000` | 按"调用方"分流：来自控制端用 100x/200x 体系，来自被管控端用 40xxx 体系（用路由前缀区分即可） |
| **绑定流程衔接** | `POST /devices/bind {deviceId, alias}` | `POST /device/bind {bindCode, …}` | 以**被管控端契约为准**：家长端生成 6 位一次性 `bindCode` → 孩子端上报 `bindCode` → 服务端返回 `deviceId/token/mqtt凭据/hmacSecret` → 通过事件通知家长端"设备已绑定" |
| **设备唯一标识** | `deviceId`（家长侧展示用） | `deviceId`（服务端下发的 UUID，全局主键） | 统一用服务端下发的 UUID 作为 `deviceId`，家长端展示的 `id` 可与之一致 |
| **模式枚举不一致** | 控制模式 `NORMAL/LEARNING/FOCUS` | 控制权限 `DEVICE_OWNER/PROFILE_OWNER/DEVICE_ADMIN/LEGACY` | 两者语义不同：前者是"使用场景模式"，后者是"系统权限等级"，服务端分别存字段，互不直接覆盖 |

---

## 2. 总体架构

```
                         ┌──────────────────────── 服务端 ────────────────────────┐
                         │                                                        │
  家长 App ──HTTPS/WS──▶ │  REST 网关 (/v1)        MQTT 接入层                    │
  (控制端)               │   ├ AuthService          ├ EMQX Broker (8883 TLS)      │
                         │   ├ DeviceService        │   topic: pg/v1/{tenant}/…    │
  孩子 App ──MQTT/HTTPS─▶ │   ├ PolicyService  ◀─────┤   下行: command/policy/config│
  (被管控端)              │   ├ CommandService ─────▶│   上行: heartbeat/ack/event  │
                         │   ├ IngestService        └ (CleanSession=false,QoS1)   │
                         │   ├ StatisticsService                                     │
                         │   ├ AlertService        WebSocket 推送 (/ws)            │
                         │   └ MessageService  ──▶ device.online/offline/          │
                         │           │                alert.new/screenshot.ready    │
                         │           ▼                                             │
                         │   PostgreSQL ──Flyway 迁移──┐  对象存储(OSS/MinIO)       │
                         │   业务表(见 §4)             │  截图/照片/录音文件          │
                         │                            ▼                            │
                         └──────────────────────── 消息总线(可选 Redis/Kafka) ──────┘
```

**三条核心链路：**
1. **指令下行**：家长点"锁屏" → `CommandService` 构造指令包（HMAC 签名）→ PUBLISH 到 `pg/v1/{tenant}/down/{deviceId}/command` → 孩子端执行 → `ack` 上报 → 更新状态 → WS 推家长。
2. **数据上行**：孩子端上报心跳/日志/事件/定位/截图（HTTPS 或 MQTT）→ `IngestService` 落库（按 `logId/eventId/msgId` 幂等去重）→ 触发统计聚合与告警生成。
3. **实时推送**：设备上线/离线、新告警、截图就绪、使用时长变更 → WS 推家长端。

---

## 3. 技术选型（主流稳妥，拒绝自造）

依据"优先成熟主流、评估生产成熟度与社区支撑"原则：

| 层 | 选型 | 理由 |
|---|---|---|
| 语言/框架 | **Spring Boot 3 + Kotlin** | 团队已是 Kotlin 系；生态最成熟、文档最多、招人/维护成本低。实时推送用 Spring WebSocket |
| 实时消息 | **EMQX**（开源 MQTT Broker） | 契约文档已引用 Headwind MDM 同款 MQTT 架构；EMQX 生产成熟、支持 8883 TLS、ACL、WebHook |
| ORM | **Spring Data JPA + Flyway** | 标准分层、迁移脚本可版本化、可回滚（符合"主流迁移方案"要求） |
| 数据库 | **PostgreSQL 16** | JSONB 存策略包/日志 payload 很顺手；事务与聚合稳定 |
| 对象存储 | **阿里云 OSS / 自建 MinIO** | 截图/照片/录音走预签名 URL 直传，服务端不中转大文件（契约 §10 待确认项 5） |
| 缓存/总线 | **Redis** | 存 bindCode、设备在线状态、JWT 黑名单、限流；也可作 WS 横向扩展的会话注册中心 |
| 认证 | **JWT（jjwt/spring-security）** | 家长端 access+refresh 双令牌；设备端独立 deviceToken |

> 备选：若追求更轻量、纯 Kotlin 栈，可用 **Ktor**。但综合"稳妥、社区、招人"，**Spring Boot 优先**。

---

## 4. 数据模型（数据库表）

基于两端 DTO + 被管控端契约，`User (1)—(N) Device` 为核心关系。核心表（PostgreSQL 思路）：

```sql
-- 用户（家长/教师/管理员）
users(id PK, phone UNIQUE, password_hash, nickname, avatar,
      role, scene_type, created_at);

-- 设备（被管控端）
devices(id PK, user_id FK, device_id UNIQUE,  -- 服务端下发的 UUID
        name, model, brand, os_version, sdk_int, app_version,
        control_mode,        -- DEVICE_OWNER/PROFILE_OWNER/DEVICE_ADMIN/LEGACY
        scene,               -- FAMILY/SCHOOL
        online_status, last_online_at, battery_level,
        latitude, longitude, group_id FK, created_at);

-- 一次性绑定码
bind_codes(code PK, user_id FK, device_id FK NULL,
           expires_at, used BOOLEAN, created_at);

-- 设备分组
device_groups(id PK, user_id FK, name, scene_type);

-- 策略包（整包 JSONB + 版本号，便于全量下发/回滚）
policies(id PK, device_id FK, version INT,
         package_json JSONB,   -- 被管控端契约 §6 整包
         scene, effective_from, effective_to, updated_at);

-- 远程指令
commands(id PK, device_id FK, msg_id UNIQUE, type, priority,
         payload_json JSONB, signature, status,   -- PENDING/SENT/EXECUTED/FAILED/DUPLICATED
         expires_at, created_at, executed_at);

-- 行为日志（孩子端批量上报）
usage_logs(id PK, device_id FK, log_id UNIQUE, type,
           timestamp, payload_json JSONB, received_at);

-- 告警事件（孩子端上行 event）
events(id PK, device_id FK, event_id UNIQUE, type, level,
       detail_json JSONB, timestamp);

-- 告警（服务端生成，供家长查看）
alerts(id PK, device_id FK, device_name, level, title, message,
       category, status,   -- ACTIVE/ACKNOWLEDGED/RESOLVED/CLOSED
       triggered_at, acknowledged_at, resolved_at);

-- 定位轨迹
locations(id PK, device_id FK, lat, lng, accuracy, provider, ts);

-- 截图/媒体
screenshots(id PK, device_id FK, shot_id UNIQUE, url, thumb_url,
            captured_at, trigger_type, width, height);

-- 临时解锁工单（契约 §9）
unlock_requests(request_id PK, device_id FK, package_name,
                app_label, duration_minutes, reason,
                status,   -- PENDING/APPROVED/REJECTED/EXPIRED
                created_at, decided_at);

-- 信息发布记录
messages(id PK, device_id FK, content_type, summary,
         display_seconds, full_screen, published_at);
```

**策略包存储要点**：整包以 `JSONB` 存 `policies.package_json`，每次变更 `version+1`。孩子端 `GET /device/policy?currentVersion=N` 比对版本，相同返回 `{upToDate:true}`，不同返回全量。数组字段（如 `app.whitelist`）出现即整体覆盖，其余字段缺省沿用上一版（局部更新语义）。

---

## 5. 服务端模块实现要点

### 5.1 认证模块（两套令牌体系）
- **家长端**：`POST /auth/login/password`（密码建议 RSA 加密后传输）、`/auth/login/sms`、`/auth/sms/send`、`/auth/refresh`、`/auth/logout`。签发 `accessToken`（建议 2h）+ `refreshToken`（建议 30d）。
- **设备端**：绑定成功后签发 `deviceToken`（建议 30d + refresh，契约 §10 待确认项 3）+ `mqttUsername`(=deviceId) + `mqttPassword` + `hmacSecret`（**仅下发给孩子端本地，服务端不保存明文，仅存用于验签的派生值**）。

### 5.2 设备与绑定模块
1. 家长端调用"生成绑定码" → 服务端生成 6 位 `bindCode`，Redis 存（10 分钟过期，一次性）。
2. 孩子端 `POST /device/bind {bindCode, deviceSn, fingerprint, model, controlMode, …}` → 校验码有效 → 生成 `deviceId`(UUID) + 令牌 + MQTT 凭据 + `hmacSecret` → 落 `devices` 并绑定到该家长 `user_id` → 返回契约 §2.2 令牌结构。
3. 绑定成功后，通过 WS/消息总线向家长端推 `device.online` 或"新设备已绑定"事件，家长端 `GET /devices` 即能看到。

### 5.3 策略模块
- 家长端改策略（`PUT /policies/{deviceId}/...`）→ 服务端合并成被管控端契约 §6 全量包 → `version+1` 落 `policies`。
- 变更后 PUBLISH 到 `pg/v1/{tenant}/down/{deviceId}/policy`，**payload 仅含版本号**。
- 孩子端收到后 `GET /device/policy?currentVersion=N` 拉全量。

### 5.4 指令下发模块（关键）
家长端 `POST /policies/{deviceId}/lock` 等 → `CommandService`：
```kotlin
// 伪代码：构造并下发指令包
val msgId = "cmd_" + ULID.nextULID()
val cmd = Command(
    msgId = msgId, deviceId = deviceId, type = "LOCK_SCREEN",
    priority = "HIGH",
    payload = mapOf("reason" to reason, "unlockAt" to unlockAt),
    signature = hmacSha256("$msgId|LOCK_SCREEN|$ts|${payloadJson}", hmacSecret)
)
commandRepository.save(cmd)                       // status=PENDING
mqttGateway.publish(
    topic = "pg/v1/$tenant/down/$deviceId/command",
    payload = cmd.toJson(), qos = 1
)
// 孩子端执行后上报 ack → 更新 status=EXECUTED → WS 推家长
```
- 指令包字段严格按契约 §4.1：`msgId/type/version/timestamp/expiresAt/priority/payload/signature`。
- 防重放：`expiresAt` 默认 5 分钟；`msgId` 服务端也做幂等窗。
- 分组广播：`pg/v1/{tenant}/broadcast/{groupId}/command`（课堂全员锁屏）。

### 5.5 数据上报（Ingest）
- `POST /device/logs`：批量（≤500/批），按 `logId` 去重，返回 `{accepted, duplicated}`。
- `POST /device/heartbeat` / MQTT `…/up/{deviceId}/heartbeat`：更新 `devices.online_status` + `last_online_at` + 电量/网络；心跳超时（>3×KeepAlive）置 `OFFLINE` 并推 `device.offline`。
- `POST /device/screenshot`：multipart → 存 OSS → 写 `screenshots` → 推 `screenshot.ready`。
- `POST /device/locations` / `POST /device/event`：落 `locations`/`events`；高危 event 直接生成 `alerts` 并推 `alert.new`。

### 5.6 统计聚合
基于 `usage_logs`（type=APP_USAGE 等）按日聚合出 `UsageStats` / `StatisticsReport`（日/周/月），供 `GET /statistics/{deviceId}/...`。建议用定时任务 + 增量修正，避免每次请求实时扫全表。

### 5.7 告警
来源：孩子端 event（契约 §4.4 枚举）+ 心跳超时 + 用量超阈值。生成后落 `alerts`，WS 推 `alert.new`；家长端 `POST /alerts/{id}/acknowledge|close` 改状态。

### 5.8 WebSocket 实时推送
`wss://api.padguard.com/ws?token=<jwt>`：握手校验 JWT → 按 `user_id` 订阅其所有设备的事件。事件类型：`device.online/offline`、`alert.new`、`screenshot.ready`、`usage.update`。

### 5.9 临时解锁闭环（契约 §9，孩子端已就绪）
1. 孩子端 `UNLOCK_REQUEST` 日志上行 → 落 `unlock_requests`（PENDING，工单 ID=requestId）。
2. 家长端审批页看到工单 → 同意 → 下发 `TEMP_UNLOCK`（payload 带 `packageName`）；拒绝 → 下发 `SHOW_MESSAGE`（payload 带 `requestId` + 拒因 `body`）。
3. 状态回写 `unlock_requests`，家长端可看历史。

---

## 6. MQTT Broker 部署（EMQX）

- **部署**：Docker 起 EMQX，`8883` 开 TLS（生产），测试用 `1883`。
- **Topic 规范**：`pg/v1/{tenantId}/{direction}/{deviceId}/{type}`，见契约 §3.2。
- **连接参数**：`ClientId = pgc_{deviceId}`、`KeepAlive=90s`、`CleanSession=false`（保留离线 QoS1 指令）、遗嘱消息 `…/up/{deviceId}/status {online:false}`（retain=true）。
- **ACL**：每个设备只能收/发自己 `deviceId` 的 topic，用 `mqttUsername=deviceId` + 密码校验 + EMQX ACL 规则实现。
- **降级**：契约 §1 规定孩子端 MQTT 不可达（校园封 8883）时自动转 HTTPS 轮询 `GET /device/commands` + `POST /device/heartbeat`，服务端两通道都要实现。
- **是否 mTLS**：初期可单向 TLS + 账号密码；高安全场景再加 mTLS（契约 §10 待确认项 6）。

---

## 7. 最小可运行 MVP（建议落地顺序）

按"先端到端跑通一条主链路"原则，分阶段：

**P0（打通桥梁）**
1. Spring Boot + PostgreSQL + Flyway 起项目，两套路由前缀 `/v1` 与 `/api/v1` 都挂上。
2. 家长端 `POST /auth/login/password` → JWT。
3. 家长端"生成绑定码" + 孩子端 `POST /device/bind` → 返回令牌/MQTT 凭据。
4. 孩子端心跳（MQTT 或 HTTPS）→ 服务端更新在线状态 → 家长端 `GET /devices` 看到设备在线。
5. 策略包存 + 孩子端 `GET /device/policy` 拉取（版本比对）。
6. 家长端一键锁屏 → 经 MQTT 下发指令包 → 孩子端执行 → ack 回显状态。

→ 到此"绑定 → 策略 → 锁屏"端到端跑通，系统算"完整运行起来"。

**P1（完整能力）**
7. 截图/拍照/位置/录屏（`/monitor`）。
8. 统计聚合（`/statistics`）。
9. 告警（`/alerts`）+ WebSocket 实时推送。
10. 临时解锁闭环（§9）、信息发布（`/messages`）、电子围栏（`/location`）。

---

## 8. 两个端需要做的"接真线"改造

服务端只是桥梁，两端也要把"Mock 假线"换成"真线"：

**被管控端（孩子）**
- 移除 `BindViewModel.DEFAULT_TEST_BIND_CODE` 与 `startBindingWithDefaultCode()`，欢迎页跳到输入码页，改调真实 `POST /device/bind`。
- 接入 Paho MQTT 客户端（按契约 §3 连接参数/topic）。
- 实现 `policy/logs/heartbeat/locations/screenshot/event` 上报（目前多为预留）。

**控制端（家长）**
- `LocalDataSource`(Mock) 替换为 `ApiDataSource`(Retrofit 真实调用)，`RepositoryModule` 注入真实实现。
- `NetworkModule` 补 `TokenManager` 拦截器，自动加 `Authorization` 头 + 失败刷新。
- 接入 `wss://.../ws` WebSocket 客户端，订阅实时事件更新 UI。

---

## 9. 安全与部署清单

- [ ] 全链路 TLS 1.2+；MQTT 走 8883。
- [ ] 指令 HMAC-SHA256 签名 + `expiresAt` 防重放 + `msgId` 幂等（契约 §8）。
- [ ] `deviceToken` 有效期 + refresh（建议 30d）。
- [ ] 截图/定位/网址仅授权家长可见，服务端侧脱敏。
- [ ] 限流：`/device/logs` 等高频接口按 `deviceId` 限流；`/auth/sms/send` 防刷。
- [ ] 数据库密码/密钥走环境变量或配置中心，不进代码库。
- [ ] 对象存储用预签名 URL 直传，避免服务端中转大文件。

---

## 10. 推荐工程目录（Spring Boot）

```
padguard-server/
├── build.gradle.kts
├── src/main/kotlin/com/padguard/server/
│   ├── config/        # Security/JWT/MQTT/WebSocket/Flyway 配置
│   ├── controller/     # rest/(v1 控制端)  api/(v1 被管控端)
│   ├── service/        # auth/device/policy/command/ingest/statistics/alert/message
│   ├── mqtt/           # MqttGateway 发布/订阅桥接
│   ├── ws/             # WebSocket 推送
│   ├── repository/     # JPA Repository
│   ├── domain/         # Entity
│   └── common/         # 响应包装/错误码/异常
├── src/main/resources/db/migration/  # Flyway 脚本
└── docker-compose.yml  # postgres + emqx + redis
```

---

## 11. 下一步建议

1. **先统一契约冲突**（§1.3）：和前端把 Base URL、错误码、绑定流程三件事拍板，避免返工。
2. **我可以直接帮你脚手架化**上面的 Spring Boot 项目（含 Flyway 建表、两套路由、JWT、EMQX 接入、P0 的 bind+policy+lock 链路示例），让你本地 `docker-compose up` 就能跑通第一条主链路。
3. 确定 Base URL 域名（`api.padguard.com` vs `api.padguard.cn`）与租户隔离策略（单租户可省略 `tenantId`）。

> 需要我继续：① 直接生成可运行的 Spring Boot 脚手架代码？② 还是先把 §1.3 的契约冲突整理成一份"前后端对齐确认表"发给你和前端对齐？
